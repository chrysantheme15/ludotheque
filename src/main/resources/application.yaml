spring:
  application:
    name: ludotheque

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=LUDOTHEQUE_BD;encrypt=false;trustServerCertificate=true
    username: LUDO_USER
    password: 'LUDO_PWD123!!'
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create